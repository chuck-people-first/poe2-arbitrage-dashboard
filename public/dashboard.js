const $=s=>document.querySelector(s);let run=null,tab='best';
const name=p=>{const x=p.split('/').filter(Boolean).pop()||p;return x.replaceAll('-',' ').replaceAll('_',' ').replace(/\b\w/g,c=>c.toUpperCase())};
const fmt=n=>new Intl.NumberFormat('en-US',{maximumFractionDigits:2}).format(n);
function render(){if(!run)return;const hour=new Date(run.sourceHourUtc);$('#sourceAge').textContent=`Completed ${hour.toISOString().replace('T',' ').slice(0,16)} UTC`;$('#age').textContent=run.sourceHourUtc.slice(0,10);$('#count').textContent=run.routes.length;let rows=run.routes.filter(r=>tab==='best'||(tab==='two'?r.strategy==='two-leg-cross':r.strategy==='closed-triangle'));$('#tabTitle').textContent=tab==='best'?'Best overall':tab==='two'?'Two-leg':'Closed loops';$('#routes').innerHTML=rows.map(r=>{const legs=r.legs.map(l=>`${l.playbook.give} ${name(l.from)} → ${l.playbook.receive} ${name(l.to)}`).join(' · ');return `<tr><td><strong>${r.strategy==='closed-triangle'?'▲':'='} ${r.startUnits} ${name(r.startCurrency)} → ${r.endUnits} ${name(r.endCurrency)}</strong><div class="route-sub">${legs}</div></td><td><span class="badge ${r.strategy==='closed-triangle'?'tri':''}">${r.strategy==='closed-triangle'?'3 legs':'2 legs'} · ${Math.round(r.fillConfidence*100)}% conf.</span><div class="route-sub">Hourly signal · ${r.dataAgeHours}h age</div></td><td class="green"><strong>+${r.conservativeProfitBase.toFixed(2)} Divine</strong><div class="route-sub">haircut ${r.movementHaircutPct.toFixed(1)}%</div></td><td>${fmt(r.profitPer1mGold)}</td><td>${r.profitPerTrade.toFixed(3)}</td><td>${r.capitalRoiPct.toFixed(1)}%</td><td>${fmt(r.goldCostTotal)}</td><td>${(r.bottleneckVolumeShare*100).toFixed(1)}%</td></tr>`}).join('')||'<tr><td colspan="8">No routes match this tab.</td></tr>'}
async function load(){const r=await fetch('/dashboard-fixture.json?'+Date.now());run=await r.json();render()}document.querySelectorAll('[data-tab]').forEach(b=>b.onclick=()=>{document.querySelectorAll('[data-tab]').forEach(x=>x.classList.remove('active'));b.classList.add('active');tab=b.dataset.tab;render()});$('#refresh').onclick=load;load();
